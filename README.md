# Simple-HTTP-Server-RPI2-
A lightweight HTTP server implemented for Raspberry Pi 2 that handles TCP connections and serves text files from a predefined directory. The requested file path is provided via an HTTP GET request. If the file exists, the server returns a 200 OK response with its content; otherwise, it responds with 404 Page Not Found.
